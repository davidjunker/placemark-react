import React from "react";

const Dashboard = () => {
  return (
    <section className="section">
      Dashboard
      {/* {{> list-categories}}
  {{> add-category}} */}
    </section>
  );
};

export default Dashboard;
